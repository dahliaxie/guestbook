# Use the official Nginx image
FROM nginx:alpine

# Copy the static files from the frontend directory into the Nginx directory
COPY index.html /usr/share/nginx/html/

# Expose port 80 for serving the frontend
EXPOSE 80
